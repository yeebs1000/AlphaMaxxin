# Root-level shared SDKs (LLM providers + brokers + MCP). The v2 backend's
# own dependency list lives at backend/requirements-backend.txt — installed
# by setup.py / start.bat / start.sh. Direct dependencies only; pip resolves
# the rest (the old pinned freeze here was a leftover from the removed v1 GUI).
anthropic
google-genai
openai
mcp
requests

# Optional broker integrations — only needed if you set the matching .env
# keys (see .env.example and README.md's "Linking your broker" section).
# All fail soft (ImportError is caught) if you don't install them.
moomoo-api
ib_async
tigeropen
