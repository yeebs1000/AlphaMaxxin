absl-py==2.4.0
annotated-types==0.7.0
anyio==4.14.0
attrs==26.1.0
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
cryptography==49.0.0
distro==1.9.0
google-auth==2.55.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.18
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.27.2
packaging==26.2
protobuf==7.35.1
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
PyJWT==2.13.0
python-multipart==0.0.32
pywin32==312; sys_platform == "win32"
referencing==0.37.0
requests==2.34.2
rpds-py==2026.5.1
sniffio==1.3.1
sse-starlette==3.4.4
starlette==1.3.1
tenacity==9.1.4
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
websockets==16.0
anthropic==0.111.0
moomoo-api==10.7.6708
google-genai==2.6.0
openai

# Optional broker integrations — only needed if you set the matching .env
# keys (see .env.example and README.md's "Linking your broker" section).
# Both fail soft (ImportError is caught) if you don't install them.
ib_async
tigeropen

# The v2 backend (FastAPI/uvicorn/pydantic/etc.) has its own dependency
# list at backend/requirements-backend.txt — installed separately by
# setup.py / start.bat / start.sh. This file now covers only the
# LLM/broker SDKs shared with the rest of the project.

